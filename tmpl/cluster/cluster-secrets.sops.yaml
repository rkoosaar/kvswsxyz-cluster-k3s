# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: ${BOOTSTRAP_CLOUDFLARE_DOMAIN}
  SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}
  SECRET_GOOGLE_CLIENT_ID: ${BOOTSTRAP_SECRET_GOOGLE_CLIENT_ID}
  SECRET_GOOGLE_CLIENT_SECRET: ${BOOTSTRAP_SECRET_GOOGLE_CLIENT_SECRET}
  SECRET_TFA_WHITELIST: ${BOOTSTRAP_SECRET_TFA_WHITELIST}
